/**
 * Intro cinematic — REMOVED.
 *
 * The intro overlay ("1 planet / 8 billion people / 3.5% tipping point …") was
 * eliminated in favour of a direct landing on the live globe. Users drop
 * straight into the experience with no gate, no skip button, no localStorage
 * flag to remember.
 *
 * This file is intentionally kept empty (rather than deleted) so that
 * existing clones / branches don't break their imports during a pull; nothing
 * in the app imports from it anymore and the bundler tree-shakes it out.
 */
export {};
