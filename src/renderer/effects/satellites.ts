// Satellites (Starlink) layer was removed — live TLE propagation produced
// unreliable visuals in production (CORS variability + camera/terminator
// edge cases), and the editorial value didn't justify the network cost.
// This file is intentionally empty; the Settings toggle, state key, and
// per-frame update were unwired in the same change. Left as a stub so the
// sandbox's file-deletion restriction doesn't block the clean-up.
export {};
